# This file defines prompts with defaults for dbt initializaton.
# It is used when the `dbt init` command is invoked.
#
fixed:
  type: databricks
prompts:
  host:
    default: dbc-xxxxxxxxx.cloud.databricks.com
  token:
    hint: 'personal access token to use, dapiXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX'
    hide_input: true
  http_path:
    hint: 'HTTP path of SQL warehouse to use'
    default: /sql/1.0/warehouses/abcdef123456
  catalog:
    hint: 'initial catalog'
    default: dev_sales
  schema:
    hint: 'personal schema where dbt will build objects during development, example: neelabh_kashyap'
  threads:
    hint: 'threads to use during development, 1 or more'
    type: 'int'
    default: 4
